# electron/build

Drop `icon.ico` (Windows) and `icon.icns` (macOS) / `icon.png` (Linux) here
before running `npm run build:win`. `electron-builder` will pick them up
automatically.

Recommended size for `icon.ico`: 256×256 with multiple resolutions embedded.
